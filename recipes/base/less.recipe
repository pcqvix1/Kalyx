pkg_name="less"
pkg_version="692"
source_archive="less-692.tar.gz"
source_dir="less-692"

build_recipe() {
  ./configure --prefix=/usr --sysconfdir=/etc
  make

  if [[ "${KALYX_RUN_TESTS:-0}" == "1" ]]; then
    make check
  fi

  make install
}

