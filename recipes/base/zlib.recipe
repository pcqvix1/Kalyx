pkg_name="zlib"
pkg_version="1.3.2"
source_archive="zlib-1.3.2.tar.gz"
source_dir="zlib-1.3.2"

build_recipe() {
  ./configure --prefix=/usr
  make

  if [[ "${KALYX_RUN_TESTS:-0}" == "1" ]]; then
    make check
  fi

  make install
  rm -v /usr/lib/libz.a
}

