pkg_name="libgcrypt"
pkg_version="1.12.0"
source_archive="libgcrypt-1.12.0.tar.bz2"
source_dir="libgcrypt-1.12.0"

build_recipe() {
  ./configure --prefix=/usr
  make

  if [[ "${KALYX_RUN_TESTS:-0}" == "1" ]]; then
    make check
  fi

  make install
}
