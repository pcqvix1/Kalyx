pkg_name="bc"
pkg_version="7.0.3"
source_archive="bc-7.0.3.tar.xz"
source_dir="bc-7.0.3"

build_recipe() {
  CC='gcc -std=c99' ./configure --prefix=/usr -G -O3 -r
  make

  if [[ "${KALYX_RUN_TESTS:-0}" == "1" ]]; then
    make test
  fi

  make install
}

