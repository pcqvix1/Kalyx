pkg_name="check"
pkg_version="0.15.2"
source_archive="check-0.15.2.tar.gz"
source_dir="check-0.15.2"

build_recipe() {
  ./configure --prefix=/usr --disable-static
  make

  if [[ "${KALYX_RUN_TESTS:-0}" == "1" ]]; then
    make check
  fi

  make docdir=/usr/share/doc/check-0.15.2 install
}

