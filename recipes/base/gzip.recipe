pkg_name="gzip"
pkg_version="1.14"
source_archive="gzip-1.14.tar.xz"
source_dir="gzip-1.14"

build_recipe() {
  ./configure --prefix=/usr
  make

  if [[ "${KALYX_RUN_TESTS:-0}" == "1" ]]; then
    make check
  fi

  make install
}

